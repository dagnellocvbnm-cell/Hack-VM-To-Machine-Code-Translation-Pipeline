This directory contains test inputs for each compilation stage.

- `vm_to_asm/` contains .vm files intended to be translated to .asm.
- `asm_to_hack/` contains .asm files intended to be translated to .hack.

These directories currently contain inputs only; expected outputs may be generated during testing.

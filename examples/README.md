This directory contains worked examples of each compilation stage.

- `vm_to_asm/` shows Virtual Machine (.vm) programs alongside their translated Hack assembly (.asm).
- `asm_to_hack/` shows Hack assembly (.asm) programs alongside their translated machine code (.hack).

Each subdirectory contains a single program and its output, intended for inspection and understanding.
